.runrail/
.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
frontend/node_modules/
dist/
*.egg-info/

# macOS
.DS_Store

# Local Claude Code settings (machine-specific, may contain local paths)
.claude/

# Built frontend bundle — generated by `cd frontend && npm run build`.
# CI rebuilds it at release time and hatch's `artifacts` config ships it
# in the wheel; the repo stays source-only.
src/runrail/web/static/
